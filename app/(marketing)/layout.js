export const metadata = {
  title: "Route Groups & Dynamic Routes",
  description: "Learn how to route to different pages.",
};

export default function MarketingLayout({ children }) {
  return <main>{children}</main>;
}
